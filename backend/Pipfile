[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = ">=3.0.0"
flask-cors = ">=4.0.0"
flask-sqlalchemy = ">=3.1.0"
marshmallow = ">=3.21.0"
psycopg2-binary = ">=2.9.9"
alembic = ">=1.13.0"
python-dotenv = ">=1.0.0"

[dev-packages]

[requires]
python_version = "3.10"
